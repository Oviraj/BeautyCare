<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="oviraj.com.beautycare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PhotoGallary" />
        <activity android:name=".HairActivity" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".SpecialOfferActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".FacialActivity" />
        <activity android:name=".BridalActivity"></activity>
    </application>

</manifest>